bin/
obj/
*.user
.vs/
.idea/
appsettings.Development.json

# Sorties du coach. Elles sont produites à partir des transcripts locaux et
# citent des prompts réels mot pour mot — le « prompt de la semaine » les
# affiche en clair. Le dépôt est public : elles n'y ont pas leur place.
bilans/

# Le sas des scènes générées, pas encore relues.
lenses/propositions/

# Paquets produits par installeur/package.ps1
*.zip
CoachingIA-portable-*/

# Archives de travail, vouées à disparaître.
_a_supprimer/

# Les épreuves tirées de vrais transcripts, et les campagnes qui les rejouent :
# elles citent des prompts réels. Le jeu partageable (evals/cas/) et l'état
# approuvé (evals/verdict.json) restent versionnés — eux ne citent personne.
evals/prive/
evals/journal/
evals/rapport.html

# Le cours « Evaluating AI Agents » : 30 Mo de labs, de captures et d'une
# solution qui n'est pas celle-ci. On le garde sous la main sur le poste, pas
# dans l'historique — il ne se retire plus une fois entré.
EvaluatingAiAgents/

# Caches Python de la chaîne eval/.
__pycache__/
