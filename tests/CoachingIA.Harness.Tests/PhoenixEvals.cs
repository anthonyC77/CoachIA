namespace CoachingIA.Harness.Tests;

/// <summary>
/// Échafaudage. À remplir par la tâche qui publie une campagne d'évaluation
/// vers le projet Phoenix coachingia-evals (spec §5) : dataset, experiment et
/// runs produits pour une campagne, AnnotatorKind CODE/LLM selon
/// IEvaluateur.Deterministe, et une épreuve qui cite du réel publiée sans son
/// contenu.
/// </summary>
public static class PhoenixEvalTests
{
    public static void Run(Action<bool, string> check)
    {
    }
}
